# bookbot

BookBot is my first [Boot.dev](https://www.boot.dev) project!



     # chars = {}
     # for c in text:
     #      lowered = c.lower()
     #      if lowered in chars:
     #        chars[lowered] += 1
     #      else:
     #        chars[lowered] = 1
     # return chars
